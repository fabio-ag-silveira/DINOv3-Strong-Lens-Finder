# Convenience install for users who don't `pip install -e .`
# Canonical path remains:  pip install -e ".[sim,demo,notebook]"

# --- Core ---
torch>=2.2
torchvision>=0.17
timm>=1.0.7
transformers>=4.56      # DINOv3 backbones
peft>=0.11              # LoRA
numpy>=1.24
scipy>=1.10
pillow>=10.0
scikit-learn>=1.3
pyyaml>=6.0
tqdm>=4.66

# --- Simulation + Bologna benchmark (FITS) ---
lenstronomy>=1.11
astropy>=5.0

# --- Demo + results notebook ---
gradio>=4.0
matplotlib>=3.6
jupyter
nbformat
